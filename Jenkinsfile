node {
    if (env.TAG_NAME !=null){
        prinln("We are building the tag and the tag name is ${env.TAG_NAME}")
    }
    else{
        println("We are building the branch")
    }

    if(env.TAG_NAME == 'release-1.0'){
        println('release tag is being build')
    }
}